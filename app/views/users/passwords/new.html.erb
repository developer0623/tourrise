<div class="sm:mx-auto sm:w-full sm:max-w-md">
  <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
    <%= t('devise.passwords.new.forgot_your_password') %>
  </h2>
</div>

<div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
  <div class="bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10">
    <%= form_for resource, as: resource_name, url: password_path(resource_name), html: { class: "space-y-6" } do |f| %>
      <%= render(Form::EmailFieldComponent.new(f, :email, options: { autofocus: true, autocomplete: "off", required: true })) %>

      <div>
        <%= render ButtonComponent.new do %>
          <%= t("devise.passwords.new.send_me_reset_password_instructions") %>
        <% end %>
      </div>

      <div class="flex items-center justify-between">
        <div class="text-sm">
          <%= link_to t("user.links.sign_in"), new_session_path(resource_name), class: "font-medium text-indigo-600 hover:text-indigo-500" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
