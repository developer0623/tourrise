<% breadcrumb :global_configurations_edit, @global_configuration %>

<div class="Content">
  <%= render 'partials/content_heading', title: "#{t("settings.global_configurations.edit.title")}" %>

  <div class="Content-main">
    <%= form_for [:settings, @global_configuration], method: "PUT", html: { class: "Form", autocomplete: :off } do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>
</div>
