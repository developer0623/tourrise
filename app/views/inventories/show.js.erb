var currentHtmlContent = document.querySelector(".js-AvailabilityCalendar");

currentHtmlContent.innerHTML = "<%= escape_javascript render(partial: "inventories/availability_calendar", locals: { inventory: @inventory }) %>";
